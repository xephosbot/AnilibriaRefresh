//
//  SearchFiltersView.swift
//  AnilibriaRefresh
//
//  Pushed via `AppRoute.searchFilters` (≈ Compose `SearchFiltersRoute`). Placeholder for now.
//

import SwiftUI

struct SearchFiltersView: View {
    var body: some View {
        FeaturePlaceholder(title: "button_filters", systemImage: "line.3.horizontal.decrease.circle")
            .navigationTitle("button_filters")
            .navigationBarTitleDisplayMode(.inline)
    }
}
