//
//  ScheduleView.swift
//  AnilibriaRefresh
//
//  Pushed via `AppRoute.schedule` (≈ Compose `ScheduleRoute`). Placeholder for now.
//

import SwiftUI

struct ScheduleView: View {
    var body: some View {
        FeaturePlaceholder(title: "label_schedule", systemImage: "calendar")
            .navigationTitle("label_schedule")
            .navigationBarTitleDisplayMode(.inline)
    }
}
