<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Allows us to override night specific attributes in the
     values-night folder. -->
    <style name="NightAdjusted.Theme.AnilibertyRefresh" parent="Theme.AppCompat.Light.NoActionBar" />

    <!-- The final theme we use -->
    <style name="Theme.AnilibertyRefresh" parent="NightAdjusted.Theme.AnilibertyRefresh" >
        <item name="android:windowLayoutInDisplayCutoutMode" tools:ignore="NewApi">default</item>
    </style>

    <style name="NightAdjusted.Theme.Splash" parent="Theme.SplashScreen.IconBackground">
        <item name="android:windowLightStatusBar" >true</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="27">true</item>
        <item name="android:windowBackground" tools:targetApi="34">@color/splash_background_light</item>
        <item name="windowSplashScreenBackground">@color/splash_background_light</item>
    </style>

    <style name="Theme.AnilibertyRefresh.Splash" parent="NightAdjusted.Theme.Splash">
        <item name="windowSplashScreenAnimatedIcon">@drawable/avd_splash</item>
        <item name="windowSplashScreenAnimationDuration">640</item>
        <item name="postSplashScreenTheme">@style/Theme.AnilibertyRefresh</item>
    </style>
</resources>