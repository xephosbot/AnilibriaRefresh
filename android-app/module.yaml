product: android/app

apply:
  - ../compose.module-template.yaml

dependencies:
  - ../shared-ui
  - $libs.androidx.activity.compose
  - $libs.androidx.core.splashscreen
  - $libs.androidx.interpolator
  - $libs.kotlinx.coroutines.android
  - $libs.koin.android

settings:
  android:
    namespace: com.xbot.anilibriarefresh
    signing: enabled